# 构建目录结构
```
- dist							# 打包后的文件
- src								# 源码
- gulpfile.js				# gulp打包配置
- package-lock.json
- package.json
- ...
```
# gulp 常用 API
```javascript
// task(taskName, fn)：创建一个基于流的任务
gulp.task("htmlHandler", function(){})

// src(PathInfo)：匹配源文件
gulp.src("./src/a.html");			// 指定目录下某个文件
gulp.src("./src/*.html");			// 指定目录下指定后缀文件
gulp.src("./src/**/*");				// 指定目录下所有文件
gulp.src("./src/**/*.html");	// 指定目录下所有指定后缀的文件

// dest(pathInfo)：放到指定目录
gulp.dest("./dist");	// 把接受的内容放到dist目录下

// watch(pathInfo, taskName)：监控指定目录下的文件，一旦发生变化，重新执行后面的任务
gulp.watch("./src/pages/*.html", "htmlHandler这个任务");	// 一旦指定目录下的.html文件发生变化，就会执行htmlHandler这个任务

// series(task1, task2, ...)：逐个执行多个任务，返回一个函数
// parallel(task1, task2, ...)：并行执行多个任务，返回一个函数

// pipe()：管道函数，接受当前流，进入下一个流过程
gulp.src().pipe(压缩任务).pipe(转码任务).pipe(gulp.dest("abc"));
```
# gulpfile.js 配置
## 插件
```shell
# 自动添加前缀
npm i gulp-autoprefixer -D

# 压缩css
npm i gulp-cssmin -D

# 将sass转成css
npm i sass gulp-sass -D

# 压缩js
npm i gulp-uglify -D

# ES6转ES5
npm i @babel/core @babel/preset-env gulp-babel -D

# 压缩html
npm i gulp-htmlmin -D

# 删除文件目录
npm i del -D

# 启动一个基于node的服务器
npm i gulp-webserver -D

# 在一个HTML页面导入一个HTML片段（组件）
npm i gulp-file-include -D
```
## 打包构建
> autoprefixer 需要自己配置
> htmlmin 需要自己配置

```javascript
const gulp = require("gulp");
const autoprefixer = require("gulp-autoprefixer");
const cssmin = require("gulp-cssmin");
const sass = require("gulp-sass")(require("sass"));
const uglify = require("gulp-uglify");
const babel = require("gulp-babel");
const htmlmin = require("gulp-htmlmin");
const del = require("del");
const webserver = require("gulp-webserver");
const fileInclude = require("gulp-file-include");

// 打包css
const cssHandler = function () {
	return gulp
		.src("./src/css/*.css")
		.pipe(autoprefixer())
		.pipe(cssmin())
		.pipe(gulp.dest("./dist/css"));
};

// 打包sass
const sassHandler = function () {
	return gulp
		.src("./src/sass/*.scss")
		.pipe(sass())
		.pipe(autoprefixer())
		.pipe(cssmin())
		.pipe(gulp.dest("./dist/sass"));
};

// 打包js
const jsHandler = function () {
	return gulp
		.src("./src/js/*.js")
		.pipe(
			babel({
				// 配置
				presets: ["@babel/env"],
			})
		)
		.pipe(uglify())
		.pipe(gulp.dest("./dist/js"));
};

// 打包html
const htmlHandler = function () {
	return gulp
		.src("./src/pages/*.html")
		.pipe(
			fileInclude({
				// 根据配置导入对应的HTML片段
				prefix: "@-@", // 自定义一个标识符
				basepath: "./src/components", // 组件目录
			})
		)
		.pipe(htmlmin())
		.pipe(gulp.dest("./dist/pages"));
};

// 打包images、video、audio、fonts、libs第三方包等...基本上都是移动位置（lib为例）
const libHandler = function () {
	return gulp.src("./src/libs/**").pipe(gulp.dest("./dist/libs"));
};

// 删除dist目录
const delHandler = function () {
	return del(["./dist"]);
};

// 启动服务器
const webHandler = function () {
	return gulp.src("./dist").pipe(
		webserver({
			host: "localhost", // 域名
			port: "8080", // 端口号
			livereload: true, // 当文件修改的时候，是否自动刷新页面
			open: "./pages/index.html", // 默认打开那个文件
			// 配置代理，每一个对象就是一个代理
			proxies: [
				{
					source: "/free", //代理标识符
					target: "https://v2.alapi.cn/api/lunar", //代理地址
				},
			],
		})
	);
};

// 监控
const watchHandler = function () {
	gulp.watch("./src/css/*.css", cssHandler);
	gulp.watch("./src/sass/*.scss", sassHandler);
	gulp.watch("./src/js/*.js", jsHandler);
	gulp.watch("./src/pages/*.html", htmlHandler);
};

module.exports.cssHandler = cssHandler;
module.exports.sassHandler = sassHandler;
module.exports.jsHandler = jsHandler;
module.exports.htmlHandler = htmlHandler;
module.exports.libHandler = libHandler;
module.exports.delHandler = delHandler;
// 配置默认任务
module.exports.default = gulp.series(
	delHandler,
	gulp.parallel(cssHandler, sassHandler, jsHandler, htmlHandler, libHandler),
	webHandler,
	watchHandler
);

```
## 组件化
```html
<header>
  <h1>我是头部组件</h1>
  <h2>titil：@-@title</h2>
  <div class="box @-@show">我是一个盒子</div>
</header>
```
```html
<!-- 引入头部组件 -->
<!-- 语法：自定义标识符include("组件路径", json对象) -->
@-@include("./header.html", {title: "首页", show: "active"})
```
# 执行任务
```shell
// gulp <taskName>
gulp sassHandler
gulp
```
