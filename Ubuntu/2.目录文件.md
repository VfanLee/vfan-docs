# 目录/文件

## 打包/压缩

`tar` 是 linux 中的打包命令。

```bash
# tar格式（该格式仅仅打包，不压缩）
tar -cvf [目标文件名].tar [原文件名/目录名] # 打包
tar -xvf [原文件名].tar                   # 解包

# tar.gz格式
tar -zcvf [目标文件名].tar.gz [原文件名/目录名] # 打包并压缩
tar -zxvf [原文件名].tar.gz                   # 解压并解包
```
