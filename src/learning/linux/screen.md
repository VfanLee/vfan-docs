# screen

```sh
# 启动一个会话
screen

# 命名会话
screen -S 会话名称

# 列出当前所有运行的 screen 会话
screen -ls

# 重新连接到会话
screen -r 会话名称或ID

# 分离会话（保持后台运行）
Ctrl + A，然后按 D

# 退出会话
1. 正常结束任务或关闭终端，会话自动结束
2. 手动退出：输入 exit 或按 Ctrl + D
```
