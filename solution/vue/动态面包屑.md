# 动态面包屑

动态面包屑指的是：根据当前 URL 自动生成面包屑导航菜单。

> [`route.match`](https://router.vuejs.org/zh/api/interfaces/RouteLocation.html#Properties-matched)：包含添加记录时被传入的 RouteRecord 的数组。它也可以包含重定向记录。不能直接使用。

## 逻辑分析

1. 根据 `route.match` 获取匹配的路由集合，进行渲染。
