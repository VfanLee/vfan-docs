# Vue

## 什么是 Vue 的响应式？

Vue 的数据响应式的设计之初是为了实现 **数据** 和 **函数** 的联动，当数据变化后，用到该数据的联动函数会自动重新运行。

在 Vue 开发中，数据和组件的 render 函数关联在一起，从而实现了数据变化时，会重新执行 render 函数。

当然除了 vue 的 render 函数之外，如：computed、watch …… 也是响应式的场景。
